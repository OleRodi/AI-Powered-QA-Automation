# Tests root duplicates

Placeholder folder for duplicate Didaxis spec files that lived under `tests/` after copies were moved to `tests/block5/`.

At cleanup time, only `tests/block5/` contained the canonical specs; no duplicate files were present to archive.
