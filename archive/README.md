# Archive

Early course work and superseded artifacts. Nothing here is part of the active Block 5 project.

## Contents

| Folder / file | Description |
|---------------|-------------|
| `todo-mvc/` | TodoMVC Playwright labs (`tests/`) and test plan output |
| `example.spec.ts` | Initial Playwright example |
| `block2/` | Block 2 prompt inputs/outputs (DS-2 through DS-5) |
| `docs/didaxis_reqs.md` | Early Didaxis test authoring notes |
| `tests-root-duplicates/` | Reserved for duplicate specs moved from `tests/` root (none at cleanup time) |

## Running archived tests

```bash
npx playwright test archive/todo-mvc/tests
npx playwright test archive/example.spec.ts
```

Active Didaxis tests live in `tests/block5/`.
